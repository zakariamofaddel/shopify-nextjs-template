{
  "name": "nextjs-shopify-v3",
  "private": true,
  "license": "MIT",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "yarn shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/app": "^3.19.0",
    "@shopify/cli": "^3.19.0"
  }
}
